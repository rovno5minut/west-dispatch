Labeled text field for lead-capture forms. Pass `multiline` for a textarea.

```jsx
<Input label="Company / MC number" placeholder="MC-123456" />
<Input label="Tell us about your fleet" multiline rows={4} />
```

Dark inset surface, 14px radius, blue focus ring (4px). Optional `hint` line below.
