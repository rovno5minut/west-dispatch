Single-open accordion for FAQ sections.

```jsx
<Accordion items={[{ q: "Do you require a contract?", a: "No long-term contracts…" }]} defaultOpen={0} />
```

Open row gets a blue border + wash and the "+" rotates to "×". Display-font questions, body-font answers.
