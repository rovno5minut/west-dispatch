Rounded surface panel — the building block for service cards, stat blocks, testimonials.

```jsx
<Card variant="default" interactive padding={28}>…</Card>
```

Variants: `default`, `raised`, `outline`, `accent` (subtle blue wash + border). Set `interactive` for a hover lift with a blue border glow. Large 20px radius, soft drop shadow, faint top sheen.
