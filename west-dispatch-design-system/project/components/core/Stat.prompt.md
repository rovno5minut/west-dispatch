Oversized metric for proof strips and stat grids.

```jsx
<Stat value="$2,840" suffix="/wk" label="Avg. revenue lift per truck" />
```

Display-font number with mono uppercase caption. `suffix` renders in the tone color (accent blue or energy crimson).
