---
name: west-dispatch-design
description: Use this skill to generate well-branded interfaces and assets for West Dispatch LLC (truck dispatching & logistics), either for production or throwaway prototypes/mocks/etc. Contains essential design guidelines, colors, type, fonts, assets, and UI kit components for prototyping.
user-invocable: true
---

Read the `readme.md` file within this skill, and explore the other available files.

If creating visual artifacts (slides, mocks, throwaway prototypes, etc), copy assets out and create static HTML files for the user to view. If working on production code, you can copy assets and read the rules here to become an expert in designing with this brand.

If the user invokes this skill without any other guidance, ask them what they want to build or design, ask some questions, and act as an expert designer who outputs HTML artifacts _or_ production code, depending on the need.

## Quick map
- `readme.md` — brand, content fundamentals, visual foundations, iconography, full index.
- `styles.css` — link this one file to get all tokens + fonts.
- `tokens/` — colors, typography, spacing/radius/shadow/motion, fonts.
- `components/` — React primitives (Button, Badge, Card, Stat, Input, Accordion).
- `ui_kits/website/` — full responsive marketing-site recreation.
- `assets/` — logo variants (use `logo-mark-light.png` on dark backgrounds).
- `guidelines/` — visual specimen cards.

## Essentials
- Dark slate-navy canvas (`#0D1420`), electric-blue primary (`#2E6BFF`), crimson energy accent (`#F0294A`, sparing).
- Display = Space Grotesk, Body = Manrope, Mono = Space Mono (rates, IDs, eyebrows).
- Pill buttons, 20px-radius cards, hairline white-alpha borders, soft shadows + top sheen, Lucide 1.75px line icons. No emoji.
